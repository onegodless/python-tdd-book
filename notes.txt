Add geckodriver to PATH: export PATH=$PATH:/path-to-extracted-file/geckodriver

Pyc: what are those.

Create a virtualenv: virtualenv -p /usr/bin/pythonx.x *nameEnv*
activate env: source *nameEnv*/bin/activate
deactive env: deactivate

start django project: django-admin.py startproject *nameProject*
start django server: manage.py runserver
create app: python manage.py startapp *nameApp*

Start repository: git init
Add a file to .gitignonre: echo “*file_name*” >> .gitignore
git status
¿git rm -r –cached?
¿git alias?
Git config list: git config –list
git remote add origin https://github.com/onegodless/python-tdd-book.git
git push -u origin master
git automatically commit any change to tracked files: git commit -a 
